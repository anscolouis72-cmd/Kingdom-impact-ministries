[build]
  builder = "NIXPACKS"

[deploy]
  startCommand = "node server.js"
  healthcheckPath = "/api/announcements"
  healthcheckTimeout = 300
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10

[environments]
  production = { DATABASE_URL = "sqlite:///app/kingdom.db" }